<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <%= favicon_link_tag 'favicon.gif' %>
    <title>A &amp; M Creamery</title>
    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= csrf_meta_tags %>
  </head>

  <body>
    <div id="title_area">
      <%= link_to image_tag('pstripes.png'), home_path %>
    </div>
    <%= render :partial => "partials/nav" %>
    <!-- <div id = "home"> -->
    <!-- <div class="row"> -->
    
      <div class="small-12 columns" id="<%= (controller_name == "home" && !logged_in?) ? "home" : "" %>">
        <%= render :partial => "partials/flash" %>
        <%= yield %>
      </div>
    <!-- </div> -->
    <!-- </div> -->
    <footer  class="bgcolor1">
      <div class="bgcolor1 small-12 columns" id="footer">
        <%= render partial: 'partials/footer' %>
      </div>
    </footer> 
    <%= javascript_include_tag "application" %>
  </body>
</html>
