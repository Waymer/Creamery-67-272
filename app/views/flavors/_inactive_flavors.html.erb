<% if @inactive_flavors.empty? %>
  <p><em>There are no inactive flavors at this time.</em></p>
<% else %>
  <h3>Inactive Flavors</h3>
  <table class="table table-striped">
    <thead>
      <tr>
        <th width="225">Flavor Name</th>
        <% if can? :update, Flavor %> 
        <th width="75">&nbsp;</th>
        <% end %>
      </tr>
    </thead>
    <tbody>
      <% @inactive_flavors.each do |flavor| %>
        <tr>
          <td><%= flavor.name %></td>   
          <% if can? :update, Flavor %>    
          <td>
            <%= link_to(edit_flavor_path(flavor), class: 'button tiny radius warning') do %>
              &nbsp;Edit&nbsp;
            <% end %>
          </td>
          <% end %>
        </tr>
      <% end %>
    </tbody>
  </table>
  <%= will_paginate @inactive_flavors, previous_label: "Previous&nbsp;", next_label: "&nbsp;Next" %> <p>&nbsp;</p>
<% end %>